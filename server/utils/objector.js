
/**
 * Проверка является ли значение undefined или null
 * @param {*} value Значение
 * @returns {boolean} Результат
 */
export function isUndefinedOrNull(value) {
    return (value === undefined) || (value === null);
}